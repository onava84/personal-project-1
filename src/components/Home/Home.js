import React from "react";
// import { useSelector } from "react-redux";

const Home = (props) => {
  return (
    <div>
      <h1>This is the Home component</h1>
    </div>
  );
};

export default Home;
